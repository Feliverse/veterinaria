<%= form_with(model: animal) do |form| %>
  <% if animal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
        <% animal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :date_of_birth, style: "display: block" %>
    <%= form.date_field :date_of_birth %>
  </div>

  <div>
    <%= form.label :escape_attempts, style: "display: block" %>
    <%= form.number_field :escape_attempts %>
  </div>

  <div>
    <%= form.label :neutered, style: "display: block" %>
    <%= form.checkbox :neutered %>
  </div>

  <div>
    <%= form.label :weight_kg, style: "display: block" %>
    <%= form.text_field :weight_kg %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
